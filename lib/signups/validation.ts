/** Shared with the sign-up form's own validation, so the rules never drift apart. */
export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const STUDENT_ID_PATTERN = /^\d{6,10}$/;
export const ACCESS_CARD_ID_PATTERN = /^\d{5}$/;
export const MIN_PASSWORD_LENGTH = 8;
