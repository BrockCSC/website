/**
 * One Letter-aspect page: the default/fallback size components/documents/
 * document-preview.tsx starts from before a PDF, image or text version
 * reports its own real dimensions, so percentage coordinates always mean
 * the same box until then.
 */
export const LETTERHEAD_PAGE_WIDTH = 816;
export const LETTERHEAD_PAGE_MIN_HEIGHT = 1056;
