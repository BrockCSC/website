/**
 * One Letter-aspect page, shared by the letterhead renderer (server), its
 * live preview (client) and the Part 2 field-placement overlay for an
 * HTML-origin document, so percentage coordinates always mean the same box.
 */
export const LETTERHEAD_PAGE_WIDTH = 816;
export const LETTERHEAD_PAGE_MIN_HEIGHT = 1056;
